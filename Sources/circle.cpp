#include "circle.h"

Circle::Circle() : Shape(Qt::red)
{
}

void Circle::draw(QPainter &painter)
{
    //pen.setColor(QColor(255,0,0));
    //pen.setColor(Qt::red);
   //painter->setPen(pen);
   // painter.drawEllipse(m_x+2, m_y+2);
    //painter->drawEllipse(x+2, y+2, tsf.data.size-penwidth, tsf.data.size-penwidth);
}

